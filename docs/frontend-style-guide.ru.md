# Руководство по стилю фронтенда OxideRelay

## Назначение

Этот документ определяет текущий визуальный язык фронтенда OxideRelay и должен использоваться как базовая точка отсчёта для будущих UI-изменений.

Основной эталонный образец — компактная таблица пользователей на странице `Users and permissions`:

- компактная высота строк
- монохромное первое визуальное впечатление
- приглушённые поверхности на тёмном фоне
- сдержанные границы
- моноширинная типографика
- высокая плотность информации без dashboard-шума

Это руководство описывает стиль, который уже существует в кодовой базе, и уточняет, какие паттерны предпочтительны в дальнейшем.

## Решение по frontend-библиотеке

OxideRelay остаётся на кастомном CSS без UI-библиотеки компонентов (без MUI, без MUI DataGrid, без Bootstrap и без других framework дизайна-систем). Это решение окончательное и применяется к каждому экрану, включая насыщенные данными представления, такие как сетка переводов: плотность таблиц, клавиатурная навигация и пагинация решаются существующими табличными паттернами и токенами ниже, а не за счёт внедрения grid/component library. Не возвращайтесь к этому вопросу без чёткой продуктовой необходимости; если такая необходимость появится, ей нужен отдельный migration epic, а не инкрементальное добавление библиотеки.

## Ключевые визуальные принципы

1. В первую очередь — тёмный интерфейс в духе консоли.
2. Моноширинная типографика — это стандартный голос продукта.
3. Белый и серый несут основную часть UI.
4. Тёплый оттенок допустим только как деликатная обработка поверхностей, а не как доминирующий акцент.
5. Оранжевый — это вспомогательный акцент и цвет фокуса, а не цвет обрамления.
6. Плотные, читаемые макеты предпочтительнее чрезмерно просторных dashboard-отступов.
7. Карточки, таблицы и формы должны ощущаться рабочими и ориентированными на production, а не как mock.

## Канонические токены

Определены в `frontend/src/styles.css`.

### Палитра

#### Базовые значения палитры

| Токен | Значение | Использование |
| --- | --- | --- |
| `--black` | `#080808` | боковая панель, auth hero, самый глубокий тёмный фон |
| `--surface` | `#0e0e0e` | основной фон приложения |
| `--surface-raised` | `#131313` | панели, поля ввода, карточки, оболочки таблиц |
| `--surface-hover` | `#1a1a1a` | состояния hover, вложенные тёмные поверхности |
| `--border` | `#1e1e1e` | границы по умолчанию |
| `--border-strong` | `#2a2a2a` | более выраженные границы, контуры полей ввода, разделение карточек |
| `--border-dotted` | `#333333` | пунктирные разделители, малозаметные структурные разделители |
| `--text-muted` | `#555555` | низкоприоритетные метаданные, заголовки таблиц в более спокойных контекстах |
| `--text-secondary` | `#888888` | вспомогательный текст, описания, вторичные подписи |
| `--text-primary` | `#f0f0f0` | текст основного содержимого |
| `--white` | `#ffffff` | основные кнопки, контуры фокуса, элементы UI с максимальным контрастом |

#### Семантическая палитра статусов

| Токен | Значение | Использование |
| --- | --- | --- |
| `--color-danger-bg` | `rgba(220, 50, 50, 0.10)` | фон деструктивной поверхности |
| `--color-danger-border` | `rgba(220, 50, 50, 0.28)` | деструктивная граница |
| `--color-danger-text` | `#fca5a5` | цвет текста/иконок для деструктивных состояний |
| `--color-success-bg` | `rgba(74, 222, 128, 0.10)` | фон поверхности успеха |
| `--color-success-border` | `rgba(74, 222, 128, 0.28)` | граница успеха |
| `--color-success-text` | `#bbf7d0` | текст успеха |
| `--color-warning-bg` | `rgba(252, 211, 77, 0.10)` | фон поверхности предупреждения |
| `--color-warning-border` | `rgba(252, 211, 77, 0.28)` | граница предупреждения |
| `--color-warning-text` | `#fef08a` | текст предупреждения |
| `--color-info-bg` | `rgba(148, 163, 184, 0.10)` | фон информационной поверхности |
| `--color-info-border` | `rgba(148, 163, 184, 0.28)` | информационная граница |
| `--color-info-text` | `#cbd5e1` | информационный текст |

#### Тёплая обработка поверхностей

Тёплый оттенок допустим только как деликатное смешивание поверх тёмных поверхностей.

Текущие тёплые поверхности workspace выводятся из:

- `color-mix(in srgb, #fff8f1 6%, var(--surface-raised) 94%)`
- `color-mix(in srgb, #fff8ef 5% to 6%, var(--surface-hover or --surface-raised) 94% to 95%)`

Эти значения допустимы для:

- панелей workspace
- вложенных admin-карточек
- ненавязчивых поверхностей настроек проектов/пользователей

Эти значения не предназначены для:

- разделителей вкладок
- крупных рамок таблиц
- активных состояний основной навигации
- глобального page chrome

#### Правила использования цветов

1. Впечатление от UI по умолчанию должно оставаться монохромным.
2. Белый и серый должны нести структуру раньше, чем акцентные цвета.
3. Оранжевый должен появляться в основном в:
   - состояниях фокуса
   - тексте предупреждений/подсказок
   - тонких тёплых смешениях поверхностей
   - поддерживающих состояниях, связанных с delivery/import или близких к caution
4. Границы карточек, панелей и таблиц по умолчанию должны оставаться нейтральными и не должны использовать оранжевое обрамление.
5. Активные вкладки, выделение таблиц и разделители макета должны предпочитать нейтральные бело-серые решения.
6. Новые сырые цвета не должны добавляться, пока они не станут токенами в `styles.css` и не будут добавлены сюда.

### Типографика

#### Семейство шрифтов

| Токен | Значение | Использование |
| --- | --- | --- |
| `--font-mono` | `"JetBrains Mono", "Fira Code", "Cascadia Code", monospace` | шрифт продукта по умолчанию во всём app shell, формах, таблицах, метаданных и admin-контенте |

#### Типографская шкала

| Токен | Значение | Использование |
| --- | --- | --- |
| `--text-xs` | `0.72rem` | badges, eyebrows, компактные подписи, заголовки таблиц |
| `--text-sm` | `0.875rem` | вспомогательный текст, метаданные, вторичный копирайт, текст кнопок |
| `--text-md` | `1rem` | основной текст, текст полей ввода, содержимое по умолчанию |
| `--text-lg` | `1.125rem` | заголовки секций |
| `--text-xl` | `1.375rem` | более крупные заголовки карточек |
| `--text-2xl` | `1.75rem` | бренд-блок / более крупные заголовки утилитарных секций |
| `--text-3xl` | `2.25rem` | масштаб заголовка auth-формы |
| `--text-4xl` | `clamp(2rem, 3.5vw, 3.25rem)` | заголовки страниц и основные hero-заголовки |

#### Правила типографики

1. Моноширинный шрифт — это стандарт, а не просто декоративный акцент.
2. `--text-4xl` зарезервирован для заголовков страниц и заголовков hero/form в auth.
3. `--text-lg` — размер заголовка секции по умолчанию.
4. `--text-sm` должен нести большую часть вторичного операционного текста.
5. `--text-xs` следует использовать только для структурного микротекста, а не для основных блоков содержимого.

### Отступы

#### Шкала отступов

| Токен | Значение | Использование |
| --- | --- | --- |
| `--space-1` | `4px` | микроинтервалы, корректировки внутренних отступов badge |
| `--space-2` | `8px` | плотное разделение label/input, компактные промежутки между строками |
| `--space-3` | `10px` | плотные отступы компонентов, компактные карточки, плотные строки |
| `--space-4` | `14px` | внутренние отступы по умолчанию |
| `--space-5` | `18px` | средний ритм карточек |
| `--space-6` | `22px` | ритм внутренних отступов панелей, промежутки между блоками страницы |
| `--space-8` | `32px` | более крупные отступы макета |
| `--space-9` | `36px` | более крупные отступы панелей/auth |
| `--space-10` | `40px` | отступы hero и shell |
| `--space-12` | `48px` | чрезмерно крупные верхнеуровневые отступы, использовать редко |

#### Правила отступов

1. Отступы содержимого по умолчанию должны в основном строиться на `--space-3`, `--space-4` и `--space-6`.
2. Плотные операционные UI должны предпочитать `--space-2`, `--space-3` и `--space-4`.
3. `--space-8` и выше следует резервировать для shell, hero или разделения крупных секций.
4. Не добавляйте сырые pixel-значения, если подходящий токен уже почти полностью закрывает потребность.
5. Поля ввода, компактные таблицы и секции inspector должны смещаться в сторону более плотного конца шкалы отступов.

### Скругления и тени

#### Скругления

| Токен | Значение | Использование |
| --- | --- | --- |
| `--radius-sm` | `2px` | поля ввода, badges, компактные элементы управления |
| `--radius-md` | `4px` | панели, карточки, диалоги, приподнятые поверхности |
| `--radius-full` | `9999px` | pills, progress bars, геометрия круглого спиннера |

#### Тени

| Токен | Значение | Использование |
| --- | --- | --- |
| `--shadow-sm` | `0 2px 8px rgba(0, 0, 0, 0.25)` | небольшие приподнятые affordance-элементы |
| `--shadow-card` | `0 4px 16px rgba(0, 0, 0, 0.20)` | карточки и панели по умолчанию |
| `--shadow-panel` | `0 24px 80px rgba(0, 0, 0, 0.35), 0 4px 24px rgba(0, 0, 0, 0.22)` | только более сильный акцент shell auth/login |

#### Правила скруглений и теней

1. Почти всегда используйте только `--radius-sm` и `--radius-md`.
2. Используйте только существующие тени панелей/карточек.
3. Не вводите декоративные стеки свечения.
4. `--shadow-panel` предназначен для заметных auth-поверхностей, а не для обычных панелей workspace.

### Motion

#### Токены motion

| Токен | Значение | Использование |
| --- | --- | --- |
| `--dur-fast` | `120ms` | реакции hover/focus |
| `--dur-base` | `200ms` | обычные UI-переходы |
| `--dur-slow` | `340ms` | появление страниц и более крупные motion-раскрытия |
| `--ease-out` | `cubic-bezier(0.22, 1, 0.36, 1)` | кривая easing по умолчанию |

#### Правила motion

1. Motion должен поддерживать ясность, а не служить декором.
2. Используйте существующие короткие переходы для hover/focus и появления.
3. Избегайте наложения нескольких анимаций в одном компоненте.
4. Анимация загрузки и прогресса должна оставаться тонкой и утилитарной.

## Структура на уровне страниц

### App Shell

- Левая навигация должна оставаться визуально стабильной на всех страницах.
- Основное содержимое использует существующий `content-shell`.
- Каждая страница workspace должна начинаться с:
  - eyebrow
  - заголовка страницы
  - опциональных сопутствующих метаданных

### Предпочтительный паттерн Workspace

Предпочтительная структура для admin/workspace-страниц:

1. Header
2. Toolbar или строка вкладок
3. Одна или несколько панелей
4. Плотные области содержимого внутри этих панелей

### Двухколоночный паттерн Workspace

Для экранов настроек или admin-detail:

- левая сторона: список/таблица или основная форма
- правая сторона: детали/метаданные/inspector
- рекомендуемое визуальное соотношение:
  - `1:2` для master-detail
  - примерно `1.5:1` для формы настроек + метаданных

## Панели и поверхности

### Панель по умолчанию

- Используйте `.panel`
- Граница должна оставаться деликатной
- Фон должен быть тёмным или мягко тонированным тёплым тёмным

### Тёплая панель Workspace

Используется на страницах проектов и пользователей:

- тёплый почти белый оттенок, смешанный с тёмной поверхностью
- лёгкое приподнимание
- без агрессивного оранжевого контура

Сейчас это лучшая обработка панелей для экранов администрирования продукта, и она должна стать стандартом для похожих страниц.

### Вложенные карточки

Вложенные карточки, такие как элементы ресурсов, группы разрешений и danger-блоки, должны:

- оставаться темнее внешней панели
- использовать сдержанные границы
- не опираться на яркие акценты для построения структуры

## Таблицы

Таблица пользователей — это канонический стиль таблиц.

### Обязательные характеристики

- компактный вертикальный ритм
- приглушённые заголовки в верхнем регистре
- деликатная разделительная линия заголовка
- тёмное заполненное тело
- выбранная строка обозначается левой полосой и небольшим сдвигом фона
- без тяжёлой zebra-striping
- без чрезмерной hover-анимации

### Правила содержимого таблиц

- основная строка ячейки: жирная, короткая, высококонтрастная
- вторичная строка: приглушённая и меньшего размера
- числовые колонки должны оставаться визуально спокойными
- badges не должны доминировать в строке

### Не делайте этого

- не делайте строки высокими, если содержимое действительно этого не требует
- не используйте яркие акцентные границы по всей таблице
- не добавляйте декоративные цветовые блоки внутрь ячеек

## Вкладки

Вкладки используются в настройках проекта и inspector пользователя.

Правила:

- активное состояние должно быть монохромным или почти монохромным
- используйте подчёркивание или нижнюю границу для выделения
- разделители вокруг вкладок должны быть нейтральными, а не акцентно окрашенными
- строки вкладок должны читаться как структура, а не как декор

## Формы

### Поля ввода

- тёмный приподнятый фон
- деликатная граница
- оранжевое кольцо фокуса / оформление границы в фокусе
- текст placeholder остаётся приглушённым
- внутренние отступы у контролов по умолчанию должны быть компактными; избегайте слишком крупных полей формы в admin-workflows

### Макет формы

- располагайте поля вертикально, если нет веской причины делать двухколоночную форму
- строки из нескольких полей должны использовать существующие grid-токены
- вспомогательный текст маленький и вторичный

### Mutations

- основное действие сохранения: белая кнопка
- вторичное действие: ghost- или secondary-кнопка
- деструктивное действие: ghost danger-кнопка, визуально отделённая

## Кнопки

### Основная

- белая заливка
- чёрный текст
- зарезервирована для главного действия в секции

### Вторичная

- приглушённая приподнятая поверхность
- нейтральная граница

### Ghost

- прозрачная или почти прозрачная
- нейтральная граница

### Danger

- только для деструктивных действий
- не должна располагаться сразу рядом со стандартными действиями сохранения без пространственного разделения

## Обратная связь

### Баннеры

- используйте только существующие семантические баннеры
- success, warning, error, info
- текст должен быть коротким и операционным

### Загрузка

- UI загрузки должен быть компактным
- избегайте крупных пустых оболочек, если не заблокирована вся страница

## Текст и тон

- лаконичный
- операционный
- с минимальным маркетинговым оттенком
- без фейковых данных
- без терминологии ролей там, где ролей не существует

## Адаптивное поведение

- без горизонтального переполнения на обычных admin-страницах
- двухколоночные страницы должны схлопываться в одну колонку на более узких экранах
- таблицы при необходимости могут прокручиваться горизонтально, но формы и панели должны перестраиваться раньше

## Правила реализации

1. Предпочитайте переиспользуемые классы в `styles.css`, а не inline-стили.
2. Избегайте разовых визуальных значений, если подходящий токен уже существует.
3. При повторяющихся паттернах предпочитайте общие utility- или component-классы, а не page-local-стили.
4. Новые страницы должны переиспользовать:
   - `.panel`
   - `.toolbar`
   - `.table-shell`
   - `.badge`
   - `.banner`
   - `.project-tab`
5. Не вводите новые визуальные направления от страницы к странице.

## Текущий снимок соответствия

### Сильные совпадения

- таблица пользователей и inspector
- структура страницы настроек проекта
- карточки ресурсов и строки метаданных
- общие контролы форм и кнопки

### Частичные совпадения

- страница проектов: структурно хороша, но визуально более старая и меньше соответствует более плотному admin-паттерну
- страницы входа и сброса пароля: согласованы по палитре и типографике, но слишком сильно полагаются на inline-стили макета
- app shell: в целом согласован, но часть композиции всё ещё жёстко задана inline

### Текущие основные отклонения

1. Inline-стили всё ещё используются в макетах и auth-страницах.
2. Тёплая обработка панелей workspace дублируется на уровне отдельных страниц вместо того, чтобы быть общим page-паттерном.
3. `ProjectsPage` всё ещё использует старую композицию “create panel + project cards”, которая визуально свободнее, чем новые страницы workspace.
4. Некоторые hover- и helper-состояния всё ещё теплее, чем необходимо для преимущественно монохромного UI.
5. Общие паттерны отступов и композиции заголовков ещё не полностью вынесены в переиспользуемые классы.

## Предпочтительное следующее направление рефакторинга

1. Вынести общие классы поверхностей workspace.
2. Убрать большую часть inline-стилей макета и отступов из `AppLayout`, `LoginPage` и `ResetPasswordPage`.
3. Привести `ProjectsPage` к той же плотности и языку панелей, что и `UsersPage` и `ProjectPage`.
4. Нормализовать использование акцента так, чтобы оранжевый оставался информационным и деликатным.
